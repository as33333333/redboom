# 项目进度与待办（redboom）

> 最后更新：2026-07（会话暂停时）。下次继续直接看这份。

## 🌐 关键信息
- **公开体验链接**：https://as33333333.github.io/redboom/
- **GitHub 仓库**：https://github.com/as33333333/redboom （公开）
- **分支模型**：`main` = 线上稳定版（Pages 从它构建）｜`dev` = 日常开发
- **本地预览**：`cd ~/Desktop/redboom && python3 -m http.server 5173` → 打开 http://localhost:5173
- **gh 命令位置**：`~/.local/bin/gh`（本机临时安装，系统 keyring 已登录 as33333333）
- **上线方式**：把 dev 合并进 main 并 push，GitHub Pages 自动重建（约 1 分钟，偶尔排队更久）

## ⏳ 唯一进行中／待确认的事
- **最新改动的上线还没生效**。代码已提交并推送（main 也已合并），但暂停时 GitHub Pages 构建卡在排队（"building"，无报错、commit 正确）——**不是代码问题**。
- 涉及的最新改动：①对比篮弹窗 ②对比分析改为「任选两个」 ③去掉「拆解视频」入口。
- **下次要做**：复测构建是否 built、线上是否已是最新版。
  - 快速验证：`curl -s "https://as33333333.github.io/redboom/assets/js/shell.js" | grep -c basket-pop`（≥1 即已上线最新版）
  - 或浏览器点右上角「🧺 对比篮」，弹出卡片=最新版。

## ✅ 已完成
- 需求→设计→全栈方案→MVP 计划（3 份文档在 redboom 根目录）
- 纯 HTML 前端三页 + FastAPI 后端骨架（含 mock 回退），已上 GitHub、已配双分支
- **爆款拆解**：顶流博主/爆款视频（领域·维度排名）、右侧分析器三 Tab（数据/内容/商业，含吸引力剖析、商业结论优先）
- **挖掘热点**：领域/类型/维度/时间 全联动榜单（示例数据）
- **对比分析**：从对比篮任选 2 个 → 动态差异/问题/建议、问题↔建议联动
- **对比篮弹窗**：列表/删除/清空/快速对比
- 领域采用抖音官方向内容垂类词表（巨量星图+主流数据机构口径）

## 📋 待办（未来方向，按优先级）
1. **确认最新版上线**（见上，最优先，纯等构建）
2. **接真实抖音数据**：部署 `Evil0ctal/Douyin_TikTok_Download_API`（Docker），后端设 `DOUYIN_API_BASE`，前端 `api.js` 的 `API_BASE` 指向后端 → 从示例升级为真数据
3. **接 AI 分析**：后端设 `LLM_API_KEY`（DeepSeek/Qwen/Claude），内容分析/商业变现走真实模型
4. **分析器 SSE 流式**：AI 结论逐段渲染
5. **评论采集**（MediaCrawler）→ 内容弱势「高频评论建议」真实化
6. **抖音热榜真实数据**（TikTokDownloader）→ 挖掘热点真实榜单
7. **占位项补真实数据**：赛道趋势曲线、橱窗销量/均价、品牌合作明细
8. **「已保存」列表页**：目前点击只弹 alert，需做成真实列表
9. 前端细节打磨：配色/文案/更多示例数据/移动端细节

## 🗂️ 项目结构速查
```
redboom/
├── index.html / discover.html / compare.html   三个模块页
├── assets/css/   theme(令牌) layout components
├── assets/js/    shell store format render api analyzer breakdown discover compare
├── assets/mock/  bloggers analysis rank categories
├── backend/      FastAPI：main + douyin_client/normalize/metrics/analyzer/llm/cache/compare + prompts
├── 自媒体工作台Agent-前端设计方案.md
├── 自媒体工作台Agent-全栈技术方案与MVP.md
└── README.md
```
