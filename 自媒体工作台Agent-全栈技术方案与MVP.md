# 自媒体工作台Agent 全栈技术方案 & MVP 计划（第一版：仅抖音）

> 前端设计见《自媒体工作台Agent-前端设计方案.md》，本文档**不重复前端设计**，聚焦：全栈架构（补后端）、GitHub 依赖(skill)判断、抖音数据获取策略、**1 天可实现的 MVP 范围与开发顺序**、非 MVP 功能的前端占位策略。

---

## 1. 全栈架构总览

抖音**没有开放数据 API**，所以本产品是「爬虫工具取数 + LLM 分析 + HTML 展示」三段式：

```
┌─────────────┐   fetch/JSON   ┌──────────────────────┐   调用   ┌────────────────────┐
│  纯 HTML 前端 │ ────────────▶ │  应用后端 (FastAPI)    │ ──────▶ │ 抖音数据服务(GitHub) │
│ index/discover│ ◀──────────── │  取数编排 + 分析编排    │ ◀────── │ Douyin 爬虫 skill    │
│ /compare .html│    结果/SSE    │                      │         └────────────────────┘
└─────────────┘                │                      │   调用   ┌────────────────────┐
                               │                      │ ──────▶ │ LLM 分析服务 (API)   │
                               └──────────────────────┘ ◀────── │ 生成分析/建议(JSON)  │
                                        │ 缓存                    └────────────────────┘
                                        ▼
                                  SQLite / 文件缓存
```

**三个外部能力**（决定「哪些要下载 skill」）：
1. **抖音数据获取** → 必须用 GitHub 开源爬虫 skill（自建服务）。
2. **AI 分析**（定位/标题/优劣势/变现/建议）→ LLM API（服务依赖，非 GitHub 下载）。
3. **图表渲染** → ECharts CDN（前端，非 skill）。

---

## 2. 后端技术方案

### 2.1 选型
| 层 | 选型 | 理由 |
|---|---|---|
| 语言/框架 | **Python + FastAPI** | 主流抖音爬虫 skill 均为 Python；与数据服务同栈，集成成本最低；异步适合取数 |
| 抖音取数 | **自建数据服务**（见 §3） | 抖音无官方 API |
| AI 分析 | **LLM API**（DeepSeek / Qwen / Claude 等，可切换） | 生成结构化分析结论 |
| 缓存/存储 | **SQLite + 本地文件** | 1 天够用，免运维；缓存取数与分析结果 |
| 部署 | **Docker Compose**（应用后端 + 数据服务两容器） | 一键起，环境隔离 |

> 后端刻意做薄：核心是**编排**（取数 → 归一化 → 计算 → 调 LLM → 缓存），不重复造爬虫。

### 2.2 后端模块
```
backend/
├── main.py            FastAPI 入口 + 路由
├── douyin_client.py   调用抖音数据服务（封装 user/video/comment 拉取）
├── normalize.py       原始JSON → 统一领域模型 Blogger/Content
├── metrics.py         数据分析计算：最多/最少/平均、单位格式化
├── analyzer.py        内容分析/商业变现：拼 prompt → 调 LLM → 校验结构化输出
├── compare.py         对比分析：两账号 diff → 问题 → 建议
├── llm.py             LLM 封装（可切模型 + SSE 流式）
├── cache.py           SQLite 缓存（key: 对象id+维度）
└── prompts/           分析/变现/对比 的 prompt 模板
```

### 2.3 后端接口（与前端 §5.5 契约一致）
```
GET  /api/blogger?url=<抖音主页链接或号>      → Blogger（信息+内容列表+数据快照）
GET  /api/content?url=<作品链接>              → Content（单作品详情+数据）
POST /api/analyze  { targetType, targetId }   → Analysis（数据/内容/商业，可 SSE 流式）
POST /api/compare  { bloggerId, accountId }   → CompareSet（三维差异+问题+建议）
POST /api/saved    { type, payload }          → 保存结果
GET  /api/discover/rank?...                   → RankItem[]（v1 用热榜/示例，见占位）
```

### 2.4 分析流水线（analyzer）
```
拉取原始数据(user/posts/comments)
  → normalize 统一模型
  → metrics 计算数据分析(纯计算, 不用LLM)
  → LLM 内容分析(定位/标题/优劣势) —— 输出 {结论, evidence[], confidence}
  → LLM 商业变现(先推测依据→再打标签) —— 不确定项标 confidence 低 → 前端渲染「推测」
  → 缓存 + 返回(支持分维度先返回)
```

---

## 3. GitHub Skill 依赖判断（核心）

> 「skill」= 需从 GitHub 下载并自建的开源能力。抖音取数是唯一**必须**下载的一类；LLM/图表是服务或 CDN，不算下载 skill。

### 3.1 抖音数据获取 skill 选型

| 能力需求 | 推荐 skill | 说明 | v1 采用 |
|---|---|---|---|
| 单博主主页信息 + 作品列表 + 单作品详情 | **Evil0ctal/Douyin_TikTok_Download_API** | FastAPI+HTTPX 异步，开箱即用 API，含 `fetch_user_post`；Docker 部署；与后端同栈 | ✅ 主力 |
| 评论采集（内容弱势→高频建议）、关键词/多账号批量 | **NanmiCoder/MediaCrawler** | Playwright 自动化，多平台，含评论；**需登录 Cookie**、较重 | ⚠️ 备选/后续 |
| 热榜数据、账号/作品批量、搜索 | **JoeanAmier/TikTokDownloader** | 含**抖音热榜**采集，适合「挖掘热点」 | ⚠️ 后续接入 |

**v1 决策**：只用 **Douyin_TikTok_Download_API** 一个 skill 起步——满足爆款拆解的「博主信息 / 作品列表 / 单作品 / 评论」核心取数，且它本身就是 FastAPI，可作为 sidecar 服务被后端调用。评论/热榜/搜索等重能力放后续版本再接 MediaCrawler / TikTokDownloader。

### 3.2 各功能是否需要 skill

| 功能 | 需要的能力 | 是否需 GitHub skill | v1 数据来源 |
|---|---|---|---|
| 博主信息 / 作品列表 | 抖音取数 | ✅ Douyin_TikTok_Download_API | 真实 |
| 单作品详情 / 数据 | 抖音取数 | ✅ 同上 | 真实 |
| 数据分析（最多/最少/平均） | 纯计算 | ❌ 后端 metrics 算 | 真实（基于取数） |
| 内容分析（定位/标题/优劣势） | LLM | ❌（LLM API，非下载） | 真实 |
| 定位分析的「官方分类集合」 | 分类词表 | ❌ 静态词表 `categories.json` | 内置示例 |
| 商业变现（推测依据/标签） | LLM + 简介/橱窗信号 | ❌ LLM；橱窗销量需专门取数 | 部分真实 + 推测 |
| 内容弱势的「高频评论建议」 | 评论采集 | ⚠️ 需 MediaCrawler（后续） | v1 占位/示例 |
| 赛道趋势曲线 | 全网聚合数据 | ⚠️ 大规模采集（后续） | v1 占位 |
| 挖掘热点排行榜 | 热榜/聚合排名 | ⚠️ TikTokDownloader 热榜（后续） | v1 示例数据 |
| 对比分析 | 两账号取数 + LLM diff | ❌ 复用取数+LLM | v1 占位（逻辑通、数据示例） |

### 3.3 LLM 与图表（非下载 skill）
- **LLM**：DeepSeek / Qwen / Claude 任选，配 `API_KEY` 即用，可在 `llm.py` 一处切换。
- **ECharts**：前端 CDN，用于赛道趋势 / 对比差异条。

### 3.4 合规与风险（重要）
- 上述工具**仅用于学习研究**，需自行处理 **Cookie / 风控**，接口可能因抖音风控失效需维护。
- 部分同类仓库已因合规下架（如 `erma0/douyin` 于 2026-06 清空），选型以**在维护、可自建**为准。
- 生产化前需评估：抓取频率限流、数据合规、robots/服务条款、用户授权（尤其"个人账号"数据）。

---

## 4. 1 天可实现的 MVP 范围

> 1 天 ≈ 8–10h。原则：**打通一条端到端真实链路**（爆款拆解单对象），其余功能前端摆出来但用占位，保证演示完整 + 可继续迭代。

### 4.1 MVP 做什么（真实可用）
✅ **三页外壳 + 导航**：index/discover/compare 三页可切换、当前高亮、对比篮计数（localStorage）。
✅ **爆款拆解 · 单对象端到端**（核心）：
  - 输入抖音**博主主页链接**或**作品链接** → 左侧展示基础信息 + 作品列表。
  - 右侧分析器：
    - **数据分析**：真实（最多/最少/平均，含标题+封面+跳转，单位格式化，缺失显「暂无数据」）。
    - **内容分析**：真实 LLM（定位标签+锐评、标题规律、内容规律、优势/弱势结论）。
    - **商业变现**：部分真实（LLM 基于简介/橱窗是否存在做推测，标「推测」徽标）。
  - 分析器三状态：加载 / 完成 / 失败重试。
  - 「加入对比」「保存」按钮可用（写 localStorage）。

### 4.2 MVP 不做（本期占位，见 §5）
❌ 挖掘热点真实排行榜（用示例数据渲染 UI）。
❌ 对比分析真实计算（UI 与示例结论占位）。
❌ 赛道趋势曲线、高频评论建议、橱窗销量/均价（标「示例/占位」）。
❌ 单博主内多作品的批量 LLM 深分析（v1 分析取代表作/聚合摘要，控制耗时与 token）。

### 4.3 范围裁剪理由
- 挖掘热点/对比 依赖**大规模采集或个人账号授权**，1 天无法稳定落地 → 先占位。
- 爆款拆解单对象仅需 1 个 skill + 1 个 LLM，链路最短、演示价值最高 → 作为 MVP 主线。

---

## 5. 非 MVP 功能前端占位策略

> 全部功能**在前端都要出现**（导航、区块、控件都摆出来），只是数据/逻辑用文字占位，并明确标注状态徽标。

| 页面/区块 | 占位方式 | 标注 |
|---|---|---|
| 挖掘热点 · 筛选器 | 控件真实可点（前端联动），榜单渲染 `mock/rank.json` | 顶部条：`示例数据 · 排行榜采集开发中` |
| 挖掘热点 · 榜单行 | 用示例博主/作品填充，操作按钮可跳爆款拆解 | 每行角标「示例」 |
| 对比分析 · 选择对象 | 可选博主（来自对比篮）+ 个人账号输入框可填 | 正常 |
| 对比分析 · 多维度/问题/建议 | 渲染示例差异与示例建议文本 | 区块头「示例结论 · 待接入」 |
| 爆款拆解 · 赛道趋势 | 空曲线 + 文案 | 「趋势分析开发中」 |
| 爆款拆解 · 内容弱势(高频评论) | 展示结构，内容写占位 | 「评论采集接入后生效」 |
| 商业变现 · 橱窗销量/均价 | 字段在，值为占位 | 「暂无数据 / 待接入」 |

占位实现：统一一个 `.placeholder` 视觉样式 + `data-status="mock|coming"` 徽标组件，后续替换数据即可撤下标注，不改结构。

---

## 6. 开发顺序（1 天时间线）

| 时段 | 任务 | 产出 | 类型 |
|---|---|---|---|
| 0.5h | 起 **Douyin_TikTok_Download_API**（Docker），验证能拉到 user/post/video | 数据服务可用 | 后端 |
| 1.0h | FastAPI 骨架 + `douyin_client` + `normalize` 统一模型 | `/api/blogger` `/api/content` 返回真实数据 | 后端 |
| 1.0h | `metrics` 数据分析计算（最多/最少/平均 + 格式化） | 数据分析 JSON | 后端 |
| 1.5h | `llm.py` + `analyzer` 内容分析/商业变现 prompt（结构化输出+confidence） | `/api/analyze` 出真实分析 | 后端 |
| 2.0h | `index.html` 爆款拆解：输入→左信息/作品列表→右分析器三Tab渲染 + 三状态 | 端到端可用 | 前端 |
| 1.0h | `shell.js` 导航/对比篮 + `theme.css`/组件样式 + 单位格式化/空态 | 统一外壳 | 前端 |
| 1.0h | `discover.html` + `compare.html` 占位版（控件+示例数据+徽标） | 三页完整可演示 | 前端 |
| 0.5h | 联调、缓存、失败/空态兜底、跨页跳转与对比篮同步 | 收尾 | 全栈 |

**依赖顺序**：数据服务 → 后端取数/计算 → LLM 分析 → 爆款拆解前端 → 外壳/样式 → 占位页 → 联调。
**每步可交付**：即使后段超时，前 5 步完成即有「真实爆款拆解」可演示。

---

## 7. 后续版本（超出 1 天）
- **V1.1** 接 MediaCrawler → 真实评论 → 内容弱势高频建议、商业变现评论信号。
- **V1.2** 接 TikTokDownloader 热榜 / 搜索 → 挖掘热点真实排行榜。
- **V1.3** 对比分析真实化（个人账号授权取数 + 三维 diff + 问题↔建议关联）。
- **V1.4** 赛道趋势聚合、橱窗带货销量/均价、缓存与限流强化、多平台（小红书）。

---

## 8. 一句话总结
- **要下载的 skill 只有 1 个（抖音取数）**：`Evil0ctal/Douyin_TikTok_Download_API`；评论/热榜后续再加 `MediaCrawler` / `TikTokDownloader`。
- **AI 分析靠 LLM API**，不是 GitHub 下载。
- **1 天 MVP = 爆款拆解单对象端到端真实**（数据真算 + 内容 LLM 真分析），其余功能前端占位、可演示、可迭代。
