# 自媒体工作台Agent 前端设计方案

> 本文档基于《自媒体工作台Agent-需求文档.md》，输出：信息架构、页面结构、交互流程、原型说明、技术方案。
> 范围严格限定在三个模块（爆款拆解 / 挖掘热点 / 对比分析），不扩展新业务。

---

## 0. 设计总览

### 0.1 一句话定位
面向 ToC 创作者的「内容分析 + 账号优化」工作台。核心不是内容展示，而是**可操作的连续工作流**：

```
挖掘热点  →  爆款拆解  →  对比分析
（发现方向）  （拆解规律）  （对照自身 + 输出建议）
```

### 0.2 贯穿全局的设计约束（来自需求）
| 约束 | 设计落点 |
|---|---|
| 结果可继续被使用 | 所有卡片/行都带操作区：查看内容、拆解、加入对比、保存 |
| 结论要有依据 | 分层展示：原始内容 → 数据指标 → 分析结论；结论旁挂「引用/依据」 |
| 推测要标注 | 商业变现等推测结论统一加「推测 / 依据不足」徽标 |
| 状态可保留 | 跨模块切换保留筛选条件与分析对象 |
| 缺数据不阻断 | 单项缺失显示「暂无数据」，不影响其他分析项 |

### 0.3 三条黄金动线（跳转关系）
1. 挖掘热点榜单 →（点内容/博主）→ 爆款拆解详情
2. 爆款拆解博主主页 →（加入对比）→ 对比分析
3. 挖掘热点博主 →（加入对比）→ 对比分析

---

## 1. 信息架构（IA）

### 1.1 全局站点地图

```
自媒体工作台Agent
├── 全局外壳 App Shell
│   ├── 顶部：Logo「自媒体工作台Agent」 · 全局搜索博主 · 领域选择器 · 对比暂存篮 · 已保存
│   └── 一级导航（常驻）：爆款拆解(默认) / 挖掘热点 / 对比分析
│
├── ① 爆款拆解  Breakdown        [左：浏览/选择区  |  右：分析器 固定分屏]
│   ├── 入口A 爆款视频列表
│   ├── 入口B 拆解维度
│   │   ├── 拆解博主 → TOP博主(博主库·各领域TOP10·搜索) → 博主主页分析
│   │   └── 拆解视频 → 单个内容拆解
│   └── 右侧 分析器 Analyzer
│       ├── Tab 数据分析
│       ├── Tab 内容分析
│       └── Tab 商业变现
│
├── ② 挖掘热点  Discover         [顶：筛选器  |  主体：排行榜]
│   ├── 数据排名：总体 / 按方向·领域
│   ├── 数据类型：博主 / 内容(视频·图文)
│   └── 排名维度：点赞·浏览·收藏·涨粉 × 一天/三天/一周
│
└── ③ 对比分析  Compare          [流程式：选对象 → 多维度 → 问题 → 建议]
    ├── 分析对象：选择的博主 + 个人账号
    ├── 多维度：数据 / 内容 / 商业变现
    └── 输出：问题总结 + 建议方案(定位·内容·商业)
```

### 1.2 导航体系
- **一级导航**：三个模块，常驻、当前模块高亮、文案与模块名严格一致。
- **窄屏**：一级导航折叠为顶部 Tab（仍保 3 入口）；爆款拆解的左右分屏在窄屏改为「上下堆叠 / 分析器抽屉化」。
- **二级导航**：仅在爆款拆解内部存在（爆款视频 / 拆解博主 / 拆解视频），以 Tab 或分段控件呈现。

### 1.3 核心对象模型（前端领域模型）
统一实体是跨模块复用与跳转的基础：

```
Blogger 博主      { id, 昵称, 头像, 粉丝数, 领域标签[], 认证, 简介, 代表内容[], 数据快照 }
Content 内容      { id, 类型(视频|图文), 封面, 标题, 博主, 时长|字数|图片数, 数据(浏览/点赞/收藏), 发布时间 }
Analysis 分析结果 { 对象(博主|内容), 数据分析, 内容分析, 商业变现, 状态, 生成时间 }
CompareSet 对比集 { 博主Blogger, 个人账号Account, 三维差异, 问题[], 建议{定位,内容,商业} }
RankItem 榜单项   { 排名, 对象(博主|内容), 数据值, 时间范围, 条件快照 }
```

> 关键：**Blogger / Content 是共享对象**。挖掘热点的榜单项、爆款拆解的分析对象、对比分析的选择对象，指向同一实体，从而实现「点榜单 → 拆解 → 加入对比」的无缝流转。

### 1.4 全局共享状态（跨模块保留）
- 当前领域/方向筛选
- 挖掘热点的筛选组合（类型 / 维度 / 时间）
- 「对比暂存篮」：任意模块加入对比的对象集合
- 已保存的分析结果 / 建议方案

---

## 2. 页面结构

### 2.1 全局外壳（App Shell）

```
┌──────────────────────────────────────────────────────────────────────┐
│ 自媒体工作台Agent   [🔍 搜索博主 ]  [领域▾]      [对比篮 ②]  [已保存]  │  顶栏
├────────────┬─────────────────────────────────────────────────────────┤
│ 爆款拆解●  │                                                          │
│ 挖掘热点   │                    模块工作区                             │
│ 对比分析   │                                                          │
└────────────┴─────────────────────────────────────────────────────────┘
```
- 顶栏承载全局搜索、领域选择、对比暂存篮（带计数）、已保存入口。
- 左侧一级导航常驻；「●」表示当前高亮模块。

### 2.2 模块一 · 爆款拆解（左右分屏，右侧分析器为核心）

```
┌── 爆款拆解 ─────────────────────────────────────────────────────────┐
│ [爆款视频] [拆解博主] [拆解视频]     🔍搜博主  领域▾               二级Tab
├───────────────────────────────┬────────────────────────────────────┤
│ 左：浏览/选择区                 │ 右：分析器 Analyzer（固定）        │
│                               │ ┌ 当前分析对象：@博主/《内容》 ─┐   │
│ ▸ 爆款视频列表                 │ │ 头像 昵称 · 粉丝数 · 领域     │   │
│   ┌ 封面 标题 博主 数据 ┐      │ ├──────────────────────────────┤   │
│   │ [查看][拆解][加对比] │      │ │ 数据分析 | 内容分析 | 商业变现│  Tab
│   └─────────────────────┘      │ ├──────────────────────────────┤   │
│                               │ │                              │   │
│ ▸ TOP博主 / 博主库             │ │   分析内容（含依据/引用）     │   │
│   各领域 TOP10 · 排名维度▾     │ │                              │   │
│   ┌ 昵称 领域 粉丝 数据 代表作 │ │   [加载中/完成/失败 状态]     │   │
│   │ [进入主页][加对比]   ┐     │ │                              │   │
│   └──────────────────────┘     │ ├──────────────────────────────┤   │
│                               │ │ [保存分析结果]  [加入对比]    │   │
│ ▸ 博主主页（进入后）           │ └──────────────────────────────┘   │
│   基础信息 + 内容列表          │                                    │
│   （点内容→右侧弹「是否拆解？」）│                                    │
└───────────────────────────────┴────────────────────────────────────┘
```

关键结构点：
- **浏览对象 vs 分析对象** 双态区分：顶部标签明示「正在分析：博主 / 内容」。
- **爆款视频行**字段：封面 · 标题 · 博主 · 核心数据；操作：查看内容 / 拆解 / 加入对比。
- **博主库**：以各领域 TOP10 为主列表，`排名维度▾` 切换后同步重排；行字段：昵称 · 领域 · 粉丝数 · 近期表现 · 代表内容 · 操作。
- **博主主页**：左展示基础信息（昵称/头像/粉丝/简介/认证/领域标签）+ 内容列表（可按浏览/点赞/收藏排序）；点单个内容 → 右侧分析器顶部弹出「是否拆解当前内容？」引导。

#### 2.2.1 分析器三个 Tab 的结构

**数据分析（博主维度）** —— 指标卡 + 极值内容卡：
```
┌ 粉丝数  1.2万 ┐
├ 浏览量/篇 ─────────────────────────────────┐
│  最多 8.6万  [封面] 《标题…》  → 跳转       │
│  最少   320  [封面] 《标题…》  → 跳转       │
│  平均 1.1万                                 │
├ 点赞量/篇  最多/最少(封面+标题+跳转)/平均   │
├ 收藏量/篇  最多/最少(封面+标题+跳转)/平均   │
└─────────────────────────────────────────────┘
```
（视频维度只展示当前内容的：粉丝数 · 浏览量 · 点赞量 · 收藏量 四项数值。）
- 数值单位格式化（万/千）+ 保留原始值；缺失显示「暂无数据」。

**内容分析** —— 分块：
```
[定位分析]  标签(好物分享…) + 一句话锐评（博主：家居带货博主 / 内容：家居带货）  置顶
[发布类型]  视频 / 图文
[标题规律]  标题原文 → 语言规律 → 满足的心理/作用
[内容规律]  主题一句话 · 关联tag · 关键词 · 目标用户 · 核心需求 · (视频:时长 / 图文:字数)
[内容优势]  营销/心理学角度解释 + 可能时引用原文
[内容弱势]  结合评论建议，抽取高频建议
[赛道趋势]  热度曲线 + 当前赛道博主数量
```

**商业变现** —— 两步式：
```
第一步  变现推测依据（标注来源）
  实体关系：橱窗/商铺 · 内容关联商品 · 实体公司认证 · 私信/评论账号 · 简介含邮箱/电话
  联名合作：知名大佬/品牌联名、采访
第二步  变现方式标签
  [引流微信] 卖课/付费社群
  [橱窗带货] 销售量 · 销售均价
  [商业合作] 合作品牌所属领域/行业
  [其他标签]（按实际推测）
  ⚠ 不确定项挂「推测 / 依据不足」徽标
```

### 2.3 模块二 · 挖掘热点（筛选器 + 排行榜）

```
┌── 挖掘热点 ──────────────────────────────────────────────────────────┐
│ 数据排名: (总体) (按方向/领域 → 领域▾)                                │ 筛选栏
│ 数据类型: (博主) (内容 → 视频/图文)                                   │
│ 排名维度: (点赞)(浏览)(收藏)(涨粉)   时间: (一天)(三天)(一周)         │
├──────────────────────────────────────────────────────────────────────┤
│ 当前条件：总体 / 博主 / 涨粉量 / 一天内最多            [清空] [保存条件]│ 条件回显
├──────────────────────────────────────────────────────────────────────┤
│ #1 [头像/封面] 名称  类型  数据值 ↑  时间范围      [详情][拆解][加对比]│
│ #2 …                                                                  │ 排行榜
│ #3 …                                                                  │
└──────────────────────────────────────────────────────────────────────┘
```
- 选「按方向/领域」才出现 `领域▾`；选「内容」才出现 `视频/图文`。
- 字段随类型变化：视频显示时长，图文显示字数/图片数；内容行含封面·标题·博主·数据·拆解入口。
- 维度语义：点赞/浏览/收藏 → 主要用于内容；涨粉 → 主要用于博主（切换类型时智能提示/联动）。
- 空结果：提示「更换时间范围或方向/领域」。

### 2.4 模块三 · 对比分析（流程式）

```
┌── 对比分析 ──────────────────────────────────────────────────────────┐
│ Step1 选择对象                                                        │
│  [选择的博主]  来自博主库/爆款拆解/搜索   |  [个人账号] 搜索/录入链接/已保存
│  ┌ 博主基础信息 ┐            ┌ 个人账号基础信息 ┐                     │
│  │ 名称·领域·粉丝│    VS      │ 名称·领域·粉丝    │  数据不全→提示补充   │
│  │ 内容类型·近期 │            │ 内容类型·近期     │                     │
├──────────────────────────────────────────────────────────────────────┤
│ Step2 多维度分析（差异高亮，非并排堆数据）                            │
│  [数据分析]  粉丝/浏览/点赞/收藏 —— 差异条 + 差值                     │
│  [内容分析]  定位·发布类型·标题·方向·目标用户·核心需求·优势·弱势 差异  │
│  [商业变现]  变现方式·实体关系·关联商品·合作·联系方式 差异            │
├──────────────────────────────────────────────────────────────────────┤
│ Step3 问题总结（按 数据/内容/商业 归纳，每条挂依据）                  │
│  · 数据弱：浏览量仅为对标 12%（依据：数据差异）                        │
│  · 定位不清：无稳定标签（依据：内容标签差异）…                        │
├──────────────────────────────────────────────────────────────────────┤
│ Step4 建议方案（与问题一一对应）      [保存方案]                      │
│  [定位建议] 账号应做成什么类型                                        │
│  [内容建议] 下一步优化的内容/标题/主题/形式                          │
│  [商业建议] 适合的变现方式 / 需补齐的商业信号                         │
└──────────────────────────────────────────────────────────────────────┘
```
- 差异突出：用「差异条 / 差值 / 高低徽标」而非纯并排数字。
- 问题 → 建议**强关联**：每条建议锚定其对应问题（可点击互相定位）。

---

## 3. 交互流程

### 3.1 主工作流（跨模块串联）
```
挖掘热点  ── 设筛选(类型/维度/时间) → 榜单
   │  点榜单项「拆解」
   ▼
爆款拆解  ── 右侧分析器加载 → 数据/内容/商业 三Tab → 依据可展开
   │  「加入对比」（→ 对比暂存篮 +1）
   ▼
对比分析  ── 选博主(来自篮) + 个人账号 → 多维度差异 → 问题 → 建议 → 保存
```
切换模块时：筛选条件、分析对象、对比篮全部保留。

### 3.2 爆款拆解 · 浏览对象↔分析对象切换（关键交互）
```
在博主主页（分析对象=博主）
   └ 点击某条内容
        → 右侧分析器顶部弹提示「是否拆解当前内容？」
            ├ 是 → 分析对象切为「内容」，三Tab重新分析(视频维度)
            └ 否 → 保持博主维度分析，仅左侧预览该内容
```

### 3.3 分析器状态机
```
idle → loading →  ┌ success → 可保存 / 加入对比
                  └ failed  → 错误提示 + [重试]
```
- loading：骨架屏 + 分 Tab 局部加载（某 Tab 好了先渲染）。
- 单指标失败/缺失：该项「暂无数据」，不拖垮整个分析器。
- AI 结论支持流式渲染（逐段出结论 + 依据）。

### 3.4 挖掘热点 · 筛选联动
```
选「总体」   → 隐藏领域选择
选「按领域」 → 显示 领域▾（必选）
选「博主」   → 隐藏 视频/图文；维度智能置「涨粉」
选「内容」   → 显示 视频/图文；维度智能置「点赞/浏览/收藏」
任一变化    → 榜单刷新 + 条件回显更新 + 空态兜底提示
```

### 3.5 对比分析 · 对象选择校验
```
选博主(库/拆解/搜索) + 个人账号(搜索/链接/已保存)
   → 展示两侧基础信息卡
   → 个人账号数据不全？ 提示「补充/授权数据」（可先跳过，标注缺项）
   → 满足条件 → 生成三维差异 → 问题 → 建议
```

### 3.6 全局复用动作
| 动作 | 触发位置 | 结果 |
|---|---|---|
| 加入对比 | 爆款视频行 / 博主库 / 博主主页 / 挖掘热点榜单 / 分析器底部 | 对比篮 +1，顶栏计数更新 |
| 拆解 | 挖掘热点榜单 / 爆款视频行 | 跳爆款拆解并自动设为分析对象 |
| 保存 | 分析器 / 对比建议方案 | 存入「已保存」 |
| 查看原内容 | 各内容卡「查看」/极值内容跳转 | 打开原文/原视频（新标签或详情层） |

---

## 4. 原型说明（关键组件规格）

> 以下为线框级组件说明，作为高保真与开发的对齐依据。

### 4.1 组件清单
| 组件 | 用途 | 关键属性 |
|---|---|---|
| `shell.js`（全局外壳） | 顶栏+一级导航+对比篮 | activeModule, compareCount |
| `ContentCard` | 内容行/卡（爆款视频、榜单内容） | 封面/标题/博主/数据/type/actions |
| `BloggerRow` | 博主库/榜单博主行 | 昵称/领域/粉丝/近期/代表作/actions |
| `Analyzer` | 右侧固定分析器 | object(博主\|内容), state, activeTab |
| `MetricCard` | 指标卡（数值+单位格式化） | raw, formatted, empty |
| `ExtremeContentCard` | 最多/最少极值内容 | 数值/标题/封面/跳转 |
| `EvidenceBlock` | 依据/引用块 | source, quote |
| `MonetizeTag` | 变现标签 | type, 附加数据(销量/均价/行业), 推测徽标 |
| `SpeculativeBadge` | 「推测/依据不足」徽标 | level |
| `TrendChart` | 赛道热度曲线 | series, 博主数量 |
| `FilterBar` | 挖掘热点筛选栏 | 各维度 + 联动规则 |
| `ConditionEcho` | 当前条件回显 | 条件快照字符串 |
| `RankTable` | 排行榜 | items, type, empty态 |
| `CompareStepper` | 对比四步 | step, 校验态 |
| `DiffRow` | 对比差异行 | 左值/右值/差值/高低 |
| `ProblemAdviceLink` | 问题↔建议关联 | problemId ↔ adviceId |
| `EmptyState` | 空/暂无数据/失败 | type, action |

### 4.2 关键交互态
- **分析器**：加载(骨架) / 完成 / 失败(重试) / 部分缺失(暂无数据)。
- **依据块**：默认折叠「查看依据」，展开显示引用来源（标题/封面/评论/认证/简介/数据）。
- **推测徽标**：橙色系，hover 显示「基于 X 推测，依据强度：中」。
- **对比差异**：优/劣用色 + 箭头 + 差值百分比。

### 4.3 响应式
- ≥1280px：爆款拆解左右分屏（左 60% / 右 40%）。
- 768–1280px：分析器可收起为右侧抽屉。
- <768px：一级导航转顶部 Tab；分析器全屏抽屉；对比分析四步纵向流。

---

## 5. 技术方案（纯 HTML）

> 采用**纯 HTML 实现**：静态 HTML + CSS + 原生 JavaScript，无前端框架、无构建工具，双击 HTML 即可运行，也可直接部署到任意静态服务器 / CDN。

### 5.1 技术栈选型
| 层 | 选型 | 理由 |
|---|---|---|
| 页面 | **原生 HTML5**（多页：3 个模块 3 个 .html + 共享头部） | 无需框架，浏览器直接打开 |
| 样式 | **原生 CSS**（CSS 变量做设计令牌）；如需提速可用 **Tailwind CDN** | 一份 `theme.css` 统一令牌，免构建 |
| 交互 | **原生 JavaScript（ES Module）**；如需轻量响应式可选 **Alpine.js CDN** | 无打包，`<script type="module">` 直接引入 |
| 图表 | **ECharts CDN**（或轻量 `Chart.js`） | 赛道趋势曲线、对比差异条 |
| 数据 | **本地 mock JSON** + `fetch()`，后期换真实接口 URL 即可 | 前后端解耦，先跑通 UI |
| 存储 | **localStorage** | 保存筛选条件 / 对比篮 / 已保存结果 |
| AI 流 | **fetch + ReadableStream（SSE）** | 分析结论流式渲染（原生支持，无需库） |

> 不引入 npm / 打包器。所有第三方库走 CDN `<script>`，保证「打开即用」。

### 5.2 文件结构（多页 HTML）
```
redboom/
├── index.html            爆款拆解（默认首页）
├── discover.html         挖掘热点
├── compare.html          对比分析
├── assets/
│   ├── css/
│   │   ├── theme.css     CSS 变量：颜色/间距/字体/圆角（设计令牌）
│   │   ├── layout.css    页面外壳 / 分屏 / 响应式栅格
│   │   └── components.css 卡片/分析器/徽标/表格等组件样式
│   ├── js/
│   │   ├── shell.js      顶栏+导航+对比篮（每页共用，高亮当前模块）
│   │   ├── store.js      localStorage 封装：筛选/对比篮/已保存
│   │   ├── render.js     模板函数：contentCard()/bloggerRow()/metricCard()…
│   │   ├── format.js     单位格式化(万/千)、条件序列化
│   │   ├── analyzer.js   分析器：三Tab + 状态机 + 流式渲染
│   │   ├── breakdown.js  爆款拆解页逻辑
│   │   ├── discover.js   挖掘热点页逻辑（筛选联动）
│   │   └── compare.js    对比分析页逻辑（Stepper/Diff/建议）
│   └── mock/
│       ├── rank.json     榜单数据
│       ├── bloggers.json 博主+内容
│       └── analysis.json 分析结果样例
```
- **组件复用**：用 `render.js` 里的模板函数返回 HTML 字符串（或用 `<template>` 元素克隆），避免复制粘贴；也可用 Web Components 封装 `<analyzer-panel>` 等自定义元素。
- **共享头部**：`shell.js` 在每页 `DOMContentLoaded` 时注入顶栏与一级导航，按 `body[data-module]` 高亮当前模块。

### 5.3 状态与跨页保留（localStorage）
- 页面间是真实跳转（多页），用 **localStorage + URL query 参数** 保留状态：
  - `localStorage: globalDomain / discoverFilter / compareBasket / savedItems`
  - **URL query**：可分享、刷新不丢，如 `discover.html?scope=overall&type=blogger&metric=fans&range=1d`。
- **跳转动线**：榜单「拆解」→ `index.html?target=content&id=xxx`；「加入对比」→ 写入 `compareBasket` 并更新顶栏计数（`storage` 事件跨标签同步）。
- **对比篮**：localStorage 数组，任意页写入，`shell.js` 读取渲染计数徽标。

### 5.4 分析器与 AI 集成
- `analyzer.js` 维护状态机 `idle → loading → success/failed`，三 Tab（数据/内容/商业）**独立渲染**，某维度就绪先显示。
- **流式**：用 `fetch()` + `response.body.getReader()` 读取 SSE，逐段 `append` 结论文本，先出结论骨架再补依据（无第三方库）。
- **结构化契约**：接口返回 JSON `{ 结论, evidence[]{source, quote}, confidence }`；JS 据 `confidence` 渲染「推测 / 依据不足」徽标——不确定信息不当事实。
- **失败/降级**：某 Tab 失败只在该 Tab 显示重试按钮；缺字段渲染「暂无数据」。先接 `mock/analysis.json`，联调时把 URL 换成真实接口。

### 5.5 数据契约（接口，前端 fetch）
```
GET  /discover/rank        ?scope&domain&type&subtype&metric&range   → RankItem[]
GET  /blogger/:id          → Blogger（基础信息+内容列表+数据快照）
POST /analyze              { targetType:blogger|content, targetId }  → Analysis(可SSE)
POST /compare              { bloggerId, accountId }                  → CompareSet(问题+建议)
POST /saved                { type, payload }                         → 保存结果
```
- 联调前：以上路径指向本地 `assets/mock/*.json`；联调后：改 `js` 里 `API_BASE` 常量即可。
- 数值字段返回**原始值**，`format.js` 负责格式化（保留 raw 供排序/计算）。

### 5.6 性能与工程
- 长列表**分页 / 触底加载**（原生 IntersectionObserver），必要时手写虚拟滚动。
- 封面图 `loading="lazy"` + 占位背景。
- mock/接口结果用内存 Map 缓存，重复拆解命中缓存。
- CSS 骨架屏类（`.skeleton`）统一，避免布局抖动。
- 三页共享一套 CSS/JS，浏览器缓存复用。

### 5.7 交付里程碑（建议）
1. **M1 骨架**：`theme.css` 令牌 + `shell.js` 顶栏导航 + 三个空 HTML 页 + mock 数据。
2. **M2 爆款拆解**：`index.html` 左右分屏 + `analyzer.js` 三 Tab + 状态机（先 mock）。
3. **M3 挖掘热点**：`discover.html` 筛选联动 + 榜单 + URL/localStorage 条件持久化。
4. **M4 对比分析**：`compare.html` 四步 Stepper + Diff + 问题↔建议关联 + 保存。
5. **M5 打通动线**：跨页跳转 / 对比篮同步 / 保存 / 流式 AI / 响应式。

---

## 6. 需求覆盖对照（自检）
| 需求要点 | 落点 |
|---|---|
| 默认进入爆款拆解 | 1.2 / 默认首页 index.html |
| 分析器固定右侧·三Tab·三状态 | 2.2 / 3.3 |
| 浏览对象 vs 分析对象 + 单内容拆解引导 | 2.2 / 3.2 |
| 数据分析 最多/最少(标题+封面+跳转)/平均 | 2.2.1 / ExtremeContentCard |
| 内容分析六分块 + 引用原文 + 赛道曲线 | 2.2.1 / EvidenceBlock / TrendChart |
| 商业变现 两步 + 推测徽标 | 2.2.1 / MonetizeTag / SpeculativeBadge |
| 挖掘热点 筛选联动 + 条件回显 + 空态 | 2.3 / 3.4 |
| 对比 选对象→多维→问题→建议 + 强关联 + 保存 | 2.4 / 3.5 / ProblemAdviceLink |
| 跨模块保留状态 + 三条跳转动线 | 1.4 / 3.1 / 5.3 |
| 缺数据不阻断 / 单位格式化 | 5.4 / 5.5 |
```
